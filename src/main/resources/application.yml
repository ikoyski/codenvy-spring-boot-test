server:
  port: 8081
spring:
  application:
    name: codenvy-spring-boot-test
  jackson:
    default-property-inclusion: NON_NULL
    deserialization:
      fail-on-unknown-properties: false
management:
  endpoints:
    health:
      id: health
      sensitive: false
      enabled: true
    info:
      id: info
      sensitive: false
      enabled: true
  info:
    git:
      mode: full    
logging:
   level:
      com:
         ikoyski: DEBUG   
